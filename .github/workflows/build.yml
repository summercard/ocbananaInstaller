name: Build OpenClawInstaller

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        include:
          - os: windows-latest
            os_name: windows
            ext: .exe
          - os: macos-latest
            os_name: macos
            ext: .app
          - os: ubuntu-latest
            os_name: linux
            ext: ""

    runs-on: ${{ matrix.os }}

    steps:
    - name: 检出代码
      uses: actions/checkout@v4

    - name: 设置 Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller

    - name: 构建 Windows 版本
      if: matrix.os == 'windows-latest'
      run: |
        pyinstaller --name=OpenClawInstaller --windowed --onefile --clean --noconfirm main.py
        cd dist
        mv OpenClawInstaller.exe OpenClawInstaller-Windows-x64.exe

    - name: 构建 macOS 版本
      if: matrix.os == 'macos-latest'
      run: |
        pyinstaller --name=OpenClawInstaller --windowed --onefile --clean --noconfirm main.py
        cd dist
        if [ -f "OpenClawInstaller.app" ]; then
          zip -r OpenClawInstaller-macOS-x64.zip OpenClawInstaller.app
        else
          zip -r OpenClawInstaller-macOS-x64.zip OpenClawInstaller
        fi

    - name: 构建 Linux 版本
      if: matrix.os == 'ubuntu-latest'
      run: |
        pyinstaller --name=OpenClawInstaller --onefile --clean --noconfirm main.py
        cd dist
        tar czf OpenClawInstaller-Linux-x64.tar.gz OpenClawInstaller

    - name: 上传构建产物
      uses: actions/upload-artifact@v4
      with:
        name: OpenClawInstaller-${{ matrix.os_name }}
        path: |
          dist/OpenClawInstaller-*${{ matrix.os_name }}*
        retention-days: 30

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write

    steps:
    - name: 下载所有构建产物
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: 显示文件列表
      run: ls -R artifacts

    - name: 创建 Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          artifacts/OpenClawInstaller-windows/OpenClawInstaller-Windows-x64.exe
          artifacts/OpenClawInstaller-macos/OpenClawInstaller-macOS-x64.zip
          artifacts/OpenClawInstaller-linux/OpenClawInstaller-Linux-x64.tar.gz
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
